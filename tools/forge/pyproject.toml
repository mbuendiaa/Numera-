[project]
name = "numera-forge"
version = "1.0.0"
description = "Internal documentation and development assistant for Numera"
requires-python = ">=3.10"
dependencies = ["typer>=0.12.0", "rich>=13.0.0", "jinja2>=3.1.0"]

[project.scripts]
forge = "forge.cli:app"
